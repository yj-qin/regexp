pub fn compile(pattern : String) -> Result[RegExp, String] {
  Err("todo")
}

pub struct RegExp {
  pattern : String
}

fn matches(self : RegExp) -> Bool {
  false
}

fn replace_all(self : RegExp, src : String, repl : String) -> String {
  ""
}

fn split(self : RegExp, s : String) -> Array[String] {
  []
}
